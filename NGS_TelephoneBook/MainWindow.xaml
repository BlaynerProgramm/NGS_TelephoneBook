<Window x:Class="NGS_TelephoneBook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NGS_TelephoneBook"
        mc:Ignorable="d"
        Title="Телефонная книжка" Height="529" Width="834.206">
	<Grid>

		<ListBox Name="LBviewContacts" VerticalContentAlignment="Bottom" HorizontalAlignment="Center" Height="440" VerticalAlignment="Top" Width="806" Margin="10,50,10,0">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="380"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>
						<Border Grid.Row="0" Grid.Column="0" BorderThickness="2">
							<Image Source=""></Image>
						</Border>
						<Label Grid.Row="1" Grid.Column="0" Content="{Binding Name}"/>
						<Label Grid.Row="2" Grid.Column="0" Content="{Binding Phone}"/>
					</Grid>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</Window>
